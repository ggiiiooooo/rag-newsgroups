"""System-правила demo-режима: ответ только по найденному контексту."""

SYSTEM_RULES = """
Ты отвечаешь только на основе найденных фрагментов из базы постов.
Не выдумывай факты, имена, темы и источники, которых нет в контексте.
Если релевантных фрагментов нет — явно откажись ответить.
""".strip()

REFUSAL_NO_CONTEXT = (
    "В базе не найдено релевантных фрагментов. "
    "Ответить по данным невозможно."
)

REFUSAL_EMPTY_QUESTION = "Задайте вопрос."

# Минимальный score, чтобы считать чанк релевантным (отсекает шум TF-IDF).
# Подобран эмпирически на корпусе 20 Newsgroups:
#   - релевантные demo-запросы дают top-score ~0.28–0.39;
#   - negative-запросы (еда, рецепты) — не выше ~0.19.
# Порог 0.22 пропускает первые и отклоняет вторые (см. scripts/eval_retrieval.py).
MIN_SCORE = 0.22
